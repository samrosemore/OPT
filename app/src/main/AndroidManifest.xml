<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ncourage.markmeok">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/markmeoklogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/markmeoklogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResetPassword"/>
        <activity android:name=".FAQ" />
        <activity android:name=".Disclamer" />
        <activity android:name=".Tutorial" />
        <activity android:name=".HelpScreen" />
        <activity android:name=".NewGroup" />
        <activity android:name=".GroupListings" />
        <activity android:name=".WelcomePage">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity android:name=".MSettings" />
        <activity android:name=".MainActivity" />

    </application>

</manifest>